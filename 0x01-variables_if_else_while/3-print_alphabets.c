#include <stdio.h>
#include <time.h>
#include <stdlib.h>

#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: displays 0 for success
 */

int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++
	putchar(ch);

	for (ch = 'A'; ch <= 'Z'; ch++
	putchar(ch);

	putchar('\n');

	return (0);
}
