#include <stdio.h>
#include <time.h>
#include <stdlib.h>

#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: displays 0 for success
 */

int main(void)
{
	char low;

	for (low = 'a'; low <= 'z'; low++
	putchar(low)

	for (low = 'A'; low <= 'Z'; low++
	putchar(low);
	putchar('\n');

	return (0);
}
